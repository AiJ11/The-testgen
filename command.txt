g++ -std=c++17 test_libapplication.cpp jsCodeGenerator/jsCodegen.cpp -o test_libapplication
./test_libapplication

# 1. Generate parser files
bison -d -o parser.tab.cc parser.yy
flex -o lex.yy.cc lexer.ll

# 2. Compile integrated 
g++ -std=c++17 -Wall -Wextra \
    test_libapplication.cpp \
    jsCodeGenerator/jsCodeGen.cpp \
    parser.tab.cc lex.yy.cc \
    -o test_libapplication

# 3. Run with manual examples 
./test_libapplication

# 4. Run with parser integration (new functionality)
./test_libapplication parser

# building testgen alone
make basic
./testgen_basic

#testgen along with symbolic
make symbolic
./testgen_symbolic
